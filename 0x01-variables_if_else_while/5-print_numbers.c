#include <stdio.h>

/**
 * main - prints out 0 - 9
 * followed by a new line
 * Return: Aways 0 (Sucess)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);

}
