#include <stdio.h>
#include "main.h"
/**
 * _strcmp - function compares two strings
 * @s1: var to be compared
 * @s2: var to be compared
 * Return: zero
 */
int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);
	return (res);
}
