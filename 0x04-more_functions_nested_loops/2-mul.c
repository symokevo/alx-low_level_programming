#include "main.h"

/**
 * mul - multipy 2 integers
 * @a: int to be multiplied to b
 * @b: int to be multiplied to a
 * Return: the results
 */
int mul(int a, int b)
{
	int c;

		c = a * b;
		return (c);
}
